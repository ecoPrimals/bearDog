[package]
name = "beardog-integration"
version.workspace = true
edition = "2024"
authors = ["ecoPrimals Team"]
description = "Songbird UPA integration layer for BearDog - enabling federated service discovery and orchestration"
license = "AGPL-3.0-or-later"
repository = "https://github.com/ecoPrimals/beardog"
homepage = "https://beardog.dev"
documentation = "https://docs.beardog.dev"
readme = "README.md"
keywords = ["integration", "upa", "songbird", "federation", "orchestration"]
categories = ["network-programming", "api-bindings"]

[lints]
workspace = true

[dependencies]
# Async runtime - modern concurrent patterns
tokio = { workspace = true, features = ["full"] }
futures = { workspace = true }

# Tower Atomic client (Pure Rust IPC via Unix sockets!)
beardog-tower-atomic = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Logging and tracing
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# System metrics
sysinfo = "0.30"

# Time handling
chrono = { workspace = true }
tokio-util = "0.7"

# Synchronization primitives
parking_lot = { workspace = true }
arc-swap = "1.6"  # Lock-free atomic Arc swaps

# UUID generation
uuid = { workspace = true }

# BearDog dependencies
beardog-config = { workspace = true }
beardog-errors = { workspace = true }
beardog-discovery = { workspace = true }
beardog-tunnel = { workspace = true }
beardog-genetics = { workspace = true }
beardog-types = { workspace = true }
beardog-capabilities = { workspace = true }

# Web framework for API server
axum = { version = "0.7", features = ["macros"] }
tower = { version = "0.4", features = ["limit", "buffer", "timeout", "util"] }
tower-http = { version = "0.5", features = ["trace", "cors", "timeout"] }

[dev-dependencies]
http = "1"
tokio-test = { workspace = true }
mockito = { workspace = true }
wiremock = { workspace = true }

[features]
default = []

